# 2026-05-05 Codex Goal Prompt Contract v2

## Summary

Implemented WF-031 reduced-scope support for Codex Goal Prompt Contract v2 in
`/ai prepare goal` output.

## Background

WF-026 added Discord generation for manual Codex CLI `/goal` request files.
WF-030 validated read-only subagents as useful for optional risk-based analysis.
WF-031 standardizes the goal request file so future Codex executions are more
repeatable, auditable, and safe.

## Scope

Changed the generated `goal_request_*.md` contract and updated related workflow
documentation. No game source files, release/deploy scripts, `_Local/`, or
`node_modules/` are in scope.

## Files Changed

```text
tools/discord-orchestrator/src/services/goalPromptService.js
tools/discord-orchestrator/src/services/responseFormatter.js
tools/discord-orchestrator/README.md
_Docs/AIWorkflow/Discord_Goal_Task_Routing_Commands.md
_Docs/AIWorkflow/Codex_Goal_Prompt_Contract_v2.md
_DevLog/WorkLog/2026-05-05_Codex_Goal_Prompt_Contract_v2.md
```

## Architecture Notes

The change keeps prompt generation inside `goalPromptService.js`. Contract
sections are generated by focused helper functions so task context, project
context, scope, safety, subagent policy, validation, and completion audit remain
separate and reviewable.

## Implementation Notes

Generated files now start with a first-line `/goal` command and include the 14
Contract v2 sections. Scope and execution behavior are mode-aware for analysis,
implementation, prototype, and review.

## Review Summary

Review should check that generated files include all Contract v2 sections and
that non-goals, human decision gates, subagent policy, validation plan, stop
conditions, and completion audit are present.

## Validation Summary

Completed validation:

```text
npm --prefix tools\discord-orchestrator run register
  passed: registered /ai command

tools\discord-orchestrator\restart_bot.bat
  first run failed under normal permissions while stopping recorded PID 43636
  approved rerun with escalated permission passed

tools\discord-orchestrator\status_bot.bat
  passed: bot running as PID 38868
  stderr log had no output after restart

Generated goal request inspection
  passed for GAME-001 analysis standard
  passed for GAME-005 implementation standard
  passed for WF-021 review compact
  each generated file started with /goal and included all 14 Contract v2 sections
  mode-aware Scope, Execution Mode, and Subagent Policy phrases were confirmed

git status --short
  showed only expected workflow/docs/service changes plus two pre-existing task-state edits

git diff --check
  passed with line-ending warnings only

git diff --stat
  passed

git ls-files | findstr /I "_Local node_modules .env discord_bot.local.json"
  returned no tracked private/local files
```

Discord slash-command UI tests were not directly executed from Discord in this
Codex run. The underlying `prepareGoalPrompt` service was invoked locally with
the requested task ids, modes, and context levels, and the bot command
registration/restart/status path was validated.

## Remaining Risks

Direct Discord slash-command interaction still requires human-side Discord UI
confirmation if full end-to-end command invocation evidence is required.

## Next Tasks

Run the three Discord UI commands manually if human end-to-end slash-command
evidence is required beyond local service generation and bot registration.

## AI Assistance

Codex implemented the reduced-scope WF-031 changes under human-provided `/goal`
instructions.
